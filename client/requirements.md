## Packages
recharts | For visualizing health scores (charts)
framer-motion | For smooth page transitions and animations
prismjs | For syntax highlighting in code diffs
date-fns | For formatting dates

## Notes
Authentication is handled via Replit Auth (useAuth hook provided).
API follows the contract in @shared/routes.
Theme should be professional developer tool style (dark mode default).
